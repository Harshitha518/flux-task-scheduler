# flux-task-scheduler
A lightweight command-line task scheduler that runs shell commands at specified intervals. Easily add, pause, resume, delete tasks, and run them in the background. Perfect for automating notifications, scripts, and repetitive tasks on Unix-like systems.
